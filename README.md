# rn-agent-observer
Local runtime observability bridge for AI agents debugging React Native/Expo apps on Android — evidence-first: screenshots, UI trees, per-request network, FPS, React renders, CDP console/heap/profile, deterministic diagnosis, before/after compare.
